import 'package:flutter/material.dart';

class ShowEpisode extends StatelessWidget {
  const ShowEpisode ({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("hai"));
  }
}